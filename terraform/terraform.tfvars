# Replace with your actual GCP Project ID

project_id = "your-gcp-project-id"
region = "asia-south1"
zone = "asia-south1-a"

# Replace with the public IP address of your admin machine to allow SSH access
# You can find your IP by searching "what is my ip" in Google.
# Format must be "xxx.xxx.xxx.xxx/32"

admin_ssh_ip_range = "0.0.0.0/0" # WARNING: This is insecure. Replace with your specific IP.

